# Copie para .env e ajuste conforme o seu ambiente.

# URL base da API do Nimbus ERP.
# Execucao local (dotnet run): http://localhost:5080
# Execucao via docker compose:  http://localhost:5080
VITE_API_URL=http://localhost:5080

# Nome exibido no header e no titulo da aba.
VITE_APP_NOME=Nimbus ERP
